# v0.31.5 Strava 전체 기록 · 연도별 PR 테스트

1. ROAD 화면 → `내 Strava 분석` 진입.
2. 기존 인증이 있으면 상태가 `Strava 연결됨`인지 확인. 없으면 Client Secret 저장 후 연결.
3. `전체 ROAD 기록 스캔` 실행.
4. 목록은 최근 30개에서 멈추지 않고 Strava 전체 `sport_type=Ride` 활동을 페이지 끝까지 찾는지 확인.
5. API 429로 중단되면 현재 분석 개수가 저장되고 `전체 ROAD 계속 분석` 버튼이 나타나는지 확인.
6. 다시 실행했을 때 이미 처리한 활동은 `캐시`로 넘어가고 미분석 활동부터 이어지는지 확인.
7. 전체 분석 완료 전 `선택 연도 연동` 버튼이 비활성인지 확인.
8. 완료 후 연도 드롭다운에서 서로 다른 연도를 선택해 각 연도의 PR 값/원본 활동이 바뀌는지 확인.
9. `역대 PR 비교`에 전체 연도 중 최고값과 원본 활동이 표시되는지 확인.
10. 긴 휴식 활동이 `⚠️`로 표시되고 1시간 이상 지속 PR 후보에서는 제외되는지 확인.
11. 원하는 연도를 선택한 뒤 `선택 연도 연동` → ROAD 화면에 `Strava YYYY년 프로필 연동됨`이 표시되는지 확인.
12. Strava 연동으로 기존 목표시간/평속/컷오프 값이 변경되지 않는지 확인.
