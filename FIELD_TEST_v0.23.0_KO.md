# v0.23.0 현장 테스트

## 1) SRAM AXS 자동 스캔
1. 자전거/드레일러를 깨운다.
2. 설정 → SRAM AXS BLE 실험실 → `SRAM 검색`.
3. `SRAM ...` 장치를 누른다.
4. `준비 완료`가 나올 때까지 기다린다.
5. `캡처 시작` → 변속 버튼으로 여러 단을 천천히 변속한다. **종료할 때는 시작 기어와 다른 기어에 둔다.**
6. `종료 · 분석`을 누른다.
7. 후보가 뜨면 화면 캡처. 후보가 없어도 `진단 로그 TXT 저장` 후 TXT를 채팅에 올린다.

### 안전
- 이 화면은 characteristic WRITE를 호출하지 않는다.
- SRAM 배터리 0x2A19는 READ/NOTIFY로 읽는다.
- WRITE 권한이 있는 d905... characteristic은 목록/로그에만 기록한다.

## 2) FIT 라이더/eMTB 분석
1. 설정 → 학습 관리 → `FIT 단독 보조학습 · 여러 개 선택`.
2. 과거 Avinox FIT을 여러 개 선택한다.
3. B급 지형/파워 보조학습과 동시에 Rider/eMTB 분석이 누적된다.
4. 설정 → `라이더 · eMTB 분석`에서 확인한다.

표시 항목:
- Rider Power 파워커브: 1초/5초/10초/30초/1분/3분/5분/10분/20분/40분/60분
- 최근 12주 / 전체 최고
- 20분 이상 유효데이터가 있을 때 추정 FTP + 신뢰도
- 사람 vs 모터 기계적 에너지 기여도
- Rider work kJ, Motor output Wh, Motor output Wh/km
- 케이던스 구간별 Motor output Wh/km 참고값

주의: 케이던스별 Wh/km는 지형이 섞인 참고값이며 실제 배터리 전비로 단정하지 않는다. 배터리 SOC/모드 소비계수는 A급 FIT+ZIP 학습만 수정한다.
