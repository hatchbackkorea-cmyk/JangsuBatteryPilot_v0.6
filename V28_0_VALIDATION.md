# v0.28.0 validation

- VERSION.txt = 0.28.0
- Android resource XML 전체 파싱 정상
- Kotlin `R.id` 참조 누락 0
- 새 `PlCarbonGearAdvisor.kt`를 최소 타입 스텁과 함께 kotlinc로 컴파일 정상
- 실제 Android Gradle compile은 실행 환경 DNS가 `services.gradle.org`를 해석하지 못해 wrapper 다운로드 단계에서 중단됨
- 권장 기어는 SRAM 실제 현재 단수 판독이 아니라 현재 속도 + A+ 목표 케이던스 + 34T/10-52T/29x2.4 조합으로 계산
