# v0.30.1 그룹 라이딩 베타 설정

앱끼리 몇 km 떨어져 있어도 앞/뒤를 보려면 휴대폰 인터넷을 통한 작은 중계 서버가 필요합니다. 블루투스만으로는 그란폰도 거리에서 팀원 추적이 불가능합니다.

## 앱 동작
- 한 방 최대 20명입니다. 앱에서 `6자리 방 코드 만들기`로 코드를 만든 뒤 팀원에게 공유할 수 있습니다.
- 모든 팀원이 같은 GPX를 불러옵니다.
- 각 폰은 GPS를 GPX 진행거리(route km)로 변환합니다.
- 약 10초마다 `방 코드 / 닉네임 / 코스키 / 진행 km / GPS / 속도`를 릴레이 서버로 보냅니다.
- 같은 방 + 같은 코스키 + 최근 60초 데이터만 화면에 표시합니다.
- 내 위치보다 route km가 크면 `앞 +x.x km`, 작으면 `뒤 x.x km`로 표시합니다.

## 릴레이 서버
루트의 `group_relay_server.js`는 Node.js 18+에서 외부 패키지 없이 실행됩니다.

```bash
node group_relay_server.js
```

기본 포트는 8787이고 방 정원은 20명입니다. `GROUP_MAX_RIDERS` 환경변수로 바꿀 수 있지만 앱 기본 설계는 20명입니다. 실제 대회에서는 각 휴대폰이 접근할 수 있는 **공개 HTTPS URL**로 배포해야 합니다. 현재 서버는 2분 이상 갱신되지 않은 라이더를 자동 삭제하고 메모리에만 상태를 보관합니다.

## 개인정보
방 코드를 아는 사람은 해당 방의 라이더 위치를 조회할 수 있는 단순 베타 구조입니다. 공개 서비스로 운영하기 전에는 인증/암호화/접근제어를 추가하는 것을 권장합니다. HTTPS를 사용하세요.
