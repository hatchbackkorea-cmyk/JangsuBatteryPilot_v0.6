# v0.27.6 현장 확인

1. 코스 페이지의 `포인트별 예상 도착시각` 카드 상단에 `주행` / `정차` 두 항목이 가로로 표시되는지 확인합니다.
2. 두 항목의 글자 크기가 `선택 코스 고도 프로필` 제목과 같은 17sp bold인지 확인합니다.
3. 주행 전 속도가 없으면 `주행 계산 중`, 정차시간은 계획 충전 기준으로 표시되는지 확인합니다.
4. 주행 시작 후 이동평균 속도가 잡히면 `주행 n시간 n분`으로 바뀌는지 확인합니다.
5. 충전 완료 또는 충전 생략 확정 후 `정차` 시간이 자동으로 줄어드는지 확인합니다.
6. 기존 포인트별 도착/충전/예상출발 ETA가 그대로 유지되는지 확인합니다.
