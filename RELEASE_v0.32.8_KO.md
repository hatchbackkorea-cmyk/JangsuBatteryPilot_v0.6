# GPX Battery Copilot v0.32.8

## PC 서버 자동 복구
- v0.32.7에서 구버전 Rider Control Center 서버가 표시되는 경우를 자동 복구합니다.
- 앱 시작 시 현재 저장된 서버가 `pc_entry=true` 새 PC 서버인지 확인합니다.
- 구버전/응답 불가 서버라면 GitHub의 `rcc-server.json`에 게시된 현재 PC 주소를 확인합니다.
- 새 주소의 `/api/health`가 정상이며 `pc_entry=true`일 때만 서버 주소를 자동 교체합니다.
- Device Token, 라이더 이름, 체중, FTP, 자동동기화 설정은 유지합니다.
- 주소가 바뀐 뒤 자동동기화와 관리자폰 상태를 다시 확인합니다.
- 새 PC DB에 관리자폰 등록이 없으면 버전 글자 길게 누르기로 재등록할 수 있음을 안내합니다.

## 공개 그룹방 서버 선택 개선
- 같은 LAN PC를 우선 사용합니다.
- 외부에서는 예전에 캐시한 주소보다 현재 `rcc-server.json` 포인터를 먼저 확인합니다.
- PC Funnel 주소가 바뀌어도 앱 재설정 없이 새 주소를 따라갈 수 있습니다.

## 유지 기능
- eMTB 배터리 코파일럿
- ROAD 그란폰도 페이스 코치
- 공개 그룹방 게스트 참가
- 방 GPX 자동 다운로드/적용
- 일반 사용자 안정판 업데이트
- 관리자폰 전용 관리 메뉴
