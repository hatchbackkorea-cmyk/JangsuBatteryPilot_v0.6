# v0.28.9 현장 테스트 — Avinox 원본 상황기반 v2 재해석

1. v0.28.9 설치 후 주행 기록이 꺼진 상태에서 앱을 실행합니다.
2. 기존에 `files/avinox_proto`로 저장된 원본이 있으면 잠시 뒤 `기존 Avinox 원본 상황기반 v2 재해석 완료 · N구간` 토스트가 뜨는지 확인합니다.
3. `설정 → 학습 관리 · 원본 A+ / FIT 백업`으로 들어가 `상황기반 v2` 구간 수와 ECO/AUTO/TRAIL/TURBO별 구간 수가 표시되는지 확인합니다.
4. `♻ 기존 Avinox 원본 전부 재해석`을 눌러 원본 개수/재해석 구간/중복 제외/실패 수가 표시되는지 확인합니다.
5. 기존 v1 학습 구간 수가 재해석 전후 그대로인지 확인합니다. v2 재해석은 v1을 삭제하면 안 됩니다.
6. 같은 GPX를 테스트 모드로 열고 2페이지 모드별 주행전략에서 안내문이 `Avinox 원본 상황기반 v2 전략`으로 바뀌는지 확인합니다.
7. 충분한 v2 표본이 있는 모드는 `※` 없이 표시되고, 부족한 모드는 `※`와 `v1/이동속도 보조값` 안내가 나오는지 확인합니다.
8. ECO/AUTO/TRAIL/TURBO 도착 SOC와 ETA를 비교해 이전처럼 단순 지형평균 때문에 Turbo/Trail 값이 뒤집히는 현상이 줄었는지 확인합니다. 실제 유사상황 원본이 반대로 나타내는 경우에는 순서를 강제로 조작하지 않습니다.
9. 새 Avinox Proto 동기화를 한 뒤 동기화 메시지에 `v1 N구간 · 상황 v2 M구간`이 함께 표시되는지 확인합니다.
10. 실제 주행 1페이지 SOC 예측/충전/긴급재탐색/BLE/터치잠금은 v0.28.8과 동일하게 동작하는지 회귀 확인합니다.

안전 조건: 주행 기록 중에는 수동 원본 재해석 버튼이 비활성화되어야 합니다.
