# v0.32.3 저비용 실시간 그룹

- 이동 중 위치 전송은 기존처럼 약 1초 간격을 유지합니다.
- 속도 1 km/h 미만 정차 상태에서는 약 5초 간격으로 자동 완화합니다.
- 다시 움직이면 1초 간격으로 즉시 복귀합니다.
- 서버 0.5.6의 1Hz 통합방송과 함께 사용하면 실제 그룹 라이딩의 네트워크/CPU 사용을 크게 줄입니다.
- 기존 방 만들기/주행 전 연결 UX와 관리자폰 설정은 그대로 유지합니다.

## 관리자 시뮬레이터 참가자 연동
- 관리자폰에서 ROAD 참가자 시뮬레이터에 라이더를 추가/수정하면 Rider Control Center의 영구 가상 라이더 보관함으로 자동 upsert합니다.
- 같은 이름의 참가자는 같은 모바일 client key로 갱신되고, PC의 실시간 그룹 가상 라이더 테스트에서 바로 선택할 수 있습니다.
- 서버 보관함 참가자는 자동 삭제하지 않습니다.
