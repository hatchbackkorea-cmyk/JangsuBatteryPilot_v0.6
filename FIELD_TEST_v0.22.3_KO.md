# v0.22.3 모드별 예상 주행가능 거리 확인

1. A급 FIT+ZIP 학습을 최소 1개 이상 반영합니다.
2. 주행을 시작하고 BLE SOC가 연결된 상태에서 ECO/AUTO/TRAIL/TURBO를 전환합니다.
3. 첫 화면 컬러 모드 배너에 `모드명  XX km 주행가능`이 함께 보이는지 확인합니다.
4. 모드가 바뀌면 해당 모드의 개인학습 소비율로 거리가 다시 계산되는지 확인합니다.
5. 계획주행에서는 현재 GPX의 남은 고도 프로파일을 반영하고, 임의주행에서는 검증된 모드별 평균 %/km를 사용합니다.
6. 숫자는 현재 SOC에서 설정한 `충전권장 기준 잔량`을 남기는 기준입니다.
7. 해당 모드 A급 학습이 없으면 `학습중`, BLE SOC가 없으면 `SOC 대기`가 표시되어야 합니다.
