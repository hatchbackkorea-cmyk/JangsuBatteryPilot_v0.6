# v0.28.4 validation

- `VERSION.txt = 0.28.4`
- Avinox selected comparison mode is synchronized to `AvinoxAssistProfileStore.preferredMode()` before rebuilding the planning/test BatteryPlan.
- Avinox benchmark percentages remain excluded from BatteryLearningStore/BatteryPlan math.
- Main energy comparison labels the active self-model mode.
- Live verified BLE mode detection still supersedes the pre-ride comparison baseline.
