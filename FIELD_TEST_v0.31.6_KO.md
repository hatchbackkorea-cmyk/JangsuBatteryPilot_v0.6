# v0.31.6 PC 관리자 자동동기화 현장 테스트

1. PC에서 Rider Control Center 서버를 실행한다.
2. PC 브라우저에서 관리자 페이지에 로그인하고 `모바일 동기화`에서 본인 라이더를 선택해 Device Token을 발급한다.
3. Windows `ipconfig`에서 PC의 IPv4 주소를 확인한다. 예: `192.168.0.10`.
4. Android 앱 → 설정 → `Rider Control Center 자동동기화`에서 서버 주소를 `http://192.168.0.10:8000`처럼 입력한다.
5. 발급한 Device Token은 최초 1회 붙여넣고 이름/체중/FTP를 입력한 뒤 `연결 저장`을 누른다.
6. 상태가 `자동동기화 완료`로 바뀌는지 확인한다.
7. PC 관리자 페이지 새로고침 후 라이더 FTP/체중/파워커브와 모바일 동기화 상태가 반영됐는지 확인한다.
8. 휴대폰에서 GPX를 하나 가져오고 PC `코스`에 자동 등록되는지 확인한다.
9. 로드 완주계획을 만들고 PC `페이스 계획`에 자동 등록되는지 확인한다.
10. 주행을 종료해 ZIP이 생성되면 PC `라이딩`에 자동 등록되는지 확인한다.
11. Wi-Fi를 끄고 GPX/주행을 저장한 뒤 앱 설정에서 `대기 N건`이 증가하는지 확인한다.
12. Wi-Fi를 다시 켜 앱으로 복귀하면 대기 건이 자동 업로드되고 0건으로 줄어드는지 확인한다.

## 중요
- 휴대폰의 `127.0.0.1`은 휴대폰 자신이므로 사용할 수 없다. PC의 LAN/Wi-Fi IPv4 주소를 사용한다.
- PC 서버가 꺼져 있거나 같은 네트워크가 아니면 로컬 서버에는 즉시 업로드할 수 없다. 이 경우 폰에 대기열로 남고 다음 연결 때 재전송된다.
- 외부 어디서든 실시간 동기화하려면 이후 서버를 HTTPS 클라우드에 배포하면 앱 구조는 그대로 사용할 수 있다.
