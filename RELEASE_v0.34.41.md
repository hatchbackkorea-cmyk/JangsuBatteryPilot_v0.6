# TimeGate v0.34.41

- 코스 만들기 핵심 버튼을 `START / +CP / FINISH`로 정리하고 일시정지 버튼을 제거했습니다.
- 수동으로 누른 CP는 즉시 실제 CP로 저장합니다.
- 감속·방향전환 기반 자동 CP는 실제 CP로 자동 생성하지 않고 큰 화면 편집에서 추천 후보로만 표시합니다.
- 큰 화면 편집에서 수동 CP(노랑), 추천 적용 CP(파랑 ◆R), 미적용 추천 CP(보라 ★)를 시각적으로 구분합니다.
- 추천 CP는 지도 또는 목록에서 선택한 뒤 `선택 추천 CP 적용`으로 실제 CP에 반영할 수 있습니다.
- `저장 코스` 버튼 이름을 `코스 불러오기`로 변경했습니다.
