# v0.21.0 현장 확인

## 1. 검증 학습
1. 설정 → `검증 학습 관리 · FIT + 앱 ZIP`.
2. `✅ 검증 학습 가져오기 · FIT + 앱 ZIP` 선택.
3. 같은 주행의 Avinox `.fit` 1개와 Jangsu 앱 `_free_ride.zip` 1개를 동시에 선택.
4. 화면에서 `정식 학습 가능`인지, FIT 기준 거리/상승고도와 ZIP 거리차가 맞는지 확인.
5. `검증된 FIT + ZIP을 정식 학습` → 최종 확인 → 정식 학습.
6. 학습 목록에 `✅ 검증 FIT+ZIP`과 생성 샘플 수가 표시되는지 확인.

정식 학습 기준은 FIT=거리/고도/파워, ZIP=BLE SOC/Assist Mode입니다. 100→98%와 모드 전환 혼합 SOC 구간은 자동 제외합니다.

## 2. 충전 목표 알림
1. 설정 → `충전 도달 알림` 켜기.
2. 임의주행은 기본 목표(50~100%)를 사용. 계획주행 충전지점에서는 코스의 충전 목표가 자동 우선.
3. 주행 중 `충전 시작`을 눌러 충전 세션을 기록.
4. BLE SOC가 목표에 도달하면 소리/진동/음성 알림 확인.
5. 충전기는 앱이 제어하지 않으므로 그대로 두면 계속 충전됨.
6. 목표가 100% 미만이고 계속 충전하면 100%에서 한 번 더 알림 확인.
