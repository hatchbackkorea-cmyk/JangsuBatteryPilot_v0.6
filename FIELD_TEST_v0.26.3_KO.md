# v0.26.3 폰 단독 업데이트 테스트

## 최초 1회
1. GitHub Actions Secrets에 고정 서명키 4종이 등록되어 있어야 합니다.
2. v0.26.3 소스를 main에 올리면 `Auto Release signed APK from main` workflow가 실행됩니다.
3. GitHub Releases에 `v0.26.3` signed APK와 `.sha256` 파일이 생기는지 확인합니다.
4. 현재 설치 APK와 Release APK의 서명이 같아야 덮어쓰기 설치가 됩니다.

## 폰에서 확인
1. 앱 → 설정 → `앱 업데이트 확인 · 바로 설치`.
2. 최신 버전이면 `최신 버전입니다.` 표시.
3. 새 버전이면 업데이트 안내 → `다운로드 및 설치`.
4. 처음 한 번 `이 출처 허용`을 켜고 앱으로 돌아오면 설치 화면이 자동으로 이어져야 합니다.
5. 이후 버전부터는 USB/PC 파일복사 없이 같은 절차로 업데이트합니다.

## 자동 확인
- 앱 시작 시 마지막 성공 확인으로부터 24시간이 지났으면 백그라운드로 GitHub Release를 확인합니다.
- 새 버전이 있을 때만 팝업을 표시합니다.
- 네트워크 오류는 조용히 무시하며 다음 실행에서 다시 확인할 수 있습니다.
