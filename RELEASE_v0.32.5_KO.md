# v0.32.5 — 공개 그룹방 · 무토큰 참가 · PC 서버 자동발견

- 새 설치 참가자는 일반 Device Token을 발급받지 않아도 됩니다.
- 앱 첫 화면에서 현재 공개 그룹방을 자동 검색해 바로 보여줍니다.
- 같은 Wi-Fi에서는 PC Rider Control Center를 UDP/HTTP로 자동 발견합니다.
- 외부 네트워크에서는 GitHub의 `rcc-server.json`에 등록된 HTTPS 주소를 자동 사용합니다.
- 공개방을 누르면 이름만 입력하고 참가합니다. 서버가 해당 방에만 유효한 임시 게스트 토큰을 자동 발급합니다.
- 방에 GPX가 지정되어 있으면 참가 즉시 GPX를 자동 다운로드하고, 주행 전이면 현재 ROAD 코스로 자동 적용합니다.
- 게스트 토큰으로 관리자/모바일동기화 API에는 접근할 수 없습니다.
- 관리자폰/기존 정식 라이더의 Device Token 방식은 그대로 유지됩니다.
- 실시간 가상레이스는 이번 버전의 그룹방 흐름에서 사용하지 않습니다. Race Lab 오프라인 시뮬레이션은 유지합니다.

## 서버 요구사항
Rider Control Center PC Server v0.6.1-pc-guest-rooms 이상.
