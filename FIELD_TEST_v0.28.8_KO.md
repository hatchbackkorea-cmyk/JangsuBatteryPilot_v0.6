# v0.28.8 현장 테스트 — 변경 파일만 모바일 배포

1. v0.28.8 ZIP을 모바일 배포 페이지에서 선택합니다.
2. 확인창에 `원격 main과 비교해 변경된 파일만 업로드` 문구가 보이는지 확인합니다.
3. `.github/workflows` 체크박스는 기본 OFF 그대로 둡니다.
4. 배포를 시작하고 진행상태에 `변경분 계산 완료 · 업로드 N · 동일 M · 삭제 K`가 표시되는지 확인합니다.
5. v0.28.7 → v0.28.8처럼 작은 수정에서는 ZIP 전체 파일 수보다 실제 `업로드 N`이 크게 작은지 확인합니다.
6. 배포 완료 로그에서 `변경 업로드 N · 동일 건너뜀 M · 삭제 K`를 확인합니다.
7. GitHub Actions signed Release가 정상 시작되고 APK 다운로드/설치까지 이어지는지 확인합니다.
8. 같은 버전 Release가 이미 존재할 때는 기존 이어받기 동작이 유지되는지 확인합니다.

회귀 확인: 배포 직후 설치, `완성된 APK 설치`, 최신버전 업데이트 설치가 모두 정상인지 확인합니다.
