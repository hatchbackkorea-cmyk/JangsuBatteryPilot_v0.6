# v0.30.2 그란폰도 시뮬레이터 테스트

1. 시작 화면에서 `ROAD` 선택.
2. 대회 GPX를 먼저 불러오기.
3. `계획 만들기` 아래 `🎬 참가자 그란폰도 시뮬레이션` 선택.
4. 처음에는 실제 참가자 3~5명만 등록 권장.
5. 참가자마다 `+ 참가자 FIT` → 이름/출발지연/보급소 기본휴식 입력 → 최근 ROAD FIT 2~5개 선택.
   - FIT가 없는 참가자는 `+ 파워만`에서 20분 또는 60분 파워 입력.
6. 30배속/60배속 선택 후 재생.
7. 확인할 것:
   - 코스 평면도와 고도프로파일에 모든 참가자가 동시에 움직이는지
   - 현재 km/순위가 진행에 따라 바뀌는지
   - 보급소 기본 휴식 0분은 PASS, 3분이면 `보급소 휴식중 0:xx`로 정지하는지
   - 보급소 휴식 후 다시 출발하는지
   - FINISH 후 완주 예상순위/시간과 그래프 2개가 나타나는지
8. 이상 동작은 화면 캡처와 사용한 GPX/FIT 조합을 함께 전달.
