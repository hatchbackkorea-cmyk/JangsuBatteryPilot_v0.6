# Avinox BLE 실험실 — 첫 현장 테스트

이 기능은 **진단 전용**입니다. 읽은 값은 개인 학습과 배터리 예측에 사용되지 않습니다.

## 1차 테스트
1. AMFLOW / Avinox 자전거 전원을 켭니다.
2. Avinox 앱에서 현재 배터리 %를 확인해 숫자를 기억합니다. 예: `66%`.
3. GPX 배터리 코파일럿 → `설정` → `Avinox BLE 진단 시작`으로 들어갑니다.
4. `주변 BLE 검색`을 누릅니다.
5. Avinox/AMFLOW/DJI로 보이는 장치가 있으면 먼저 눌러봅니다. 이름 없는 장치도 후보입니다.
6. 연결되면 현재 배터리 %를 입력하고 `현재 값과 대조`를 누릅니다.
7. `BLE 진단 로그 저장`으로 TXT 파일을 저장합니다.

## 연결이 안 되면
Avinox 앱이 BLE 연결을 잡고 있을 수 있습니다.
1. Avinox 앱에서 배터리 %를 먼저 확인합니다.
2. Avinox 앱을 최근 앱 화면에서도 제거하거나 완전히 종료합니다.
3. 자전거는 켠 채로 코파일럿 BLE 실험실에서 다시 검색합니다.

## 2차 테스트 — 제일 중요
배터리가 1~2% 바뀐 뒤 다시 같은 장치에 연결합니다.
예: 66% → 65%.

- 새 배터리 % `65`를 입력하고 다시 `현재 값과 대조`
- 새 진단 로그 저장

두 로그에서 같은 Characteristic이 `66 → 65`로 같이 움직이면 매우 강한 SOC 후보입니다.

## 채팅에 보내줄 파일
`avinox_ble_YYYYMMDD_HHmmss.txt` 파일을 그대로 올려주세요.
가능하면 서로 다른 배터리 %에서 저장한 로그 2개가 가장 좋습니다.
