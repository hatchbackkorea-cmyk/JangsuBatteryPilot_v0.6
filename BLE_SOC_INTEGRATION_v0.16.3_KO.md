# v0.16.3 Avinox BLE SOC 실주행 통합

## 검증 근거
- 66%: FFF4 long packet `... 57 09 42 ...`
- 65%: FFF4 long packet `... 57 09 41 ...`
- 64%: FFF4 long packet `... 57 09 40 ...`

## 동작
1. 주행 시작 시 Avinox BLE 자동 연결
2. FFF0/FFF4 Notify에서 검증된 SOC 바이트 판독
3. 동일 SOC 2회 연속 확인 후 확정
4. SOC가 변할 때만 `BLE_AVINOX` 실제 배터리 관측값 저장
5. 충전 세션 중에는 화면만 실시간 갱신하고 RIDING 값으로 저장하지 않음
6. 연결 해제 시 자동 재검색/재연결

## UI
- 마이크 배터리 입력 버튼 제거
- 실제 배터리 수치를 크게 표시
- BLE 연결 상태를 패널 하단에 표시
- BLE 실패 시 `수동` 버튼으로 비상 입력 가능

## 주의
첫 실주행 몇 회는 이벤트 로그의 `BATTERY_BLE`와 실제 Avinox 계기판 SOC를 함께 대조해 안정성을 계속 확인합니다.

## 화면 재배치
- 마이크/되돌리기 영역 제거
- 실제 배터리 숫자를 42sp로 확대
- BLE 연결 상태를 실제 배터리 카드 안에서 즉시 표시
- 수동 입력은 BLE 장애 시 비상용으로만 유지
