# v0.30.9 현장/기능 테스트

1. ROAD 계획에서 `목표 평속`을 선택하고 값을 저장한 뒤 시뮬레이터를 연다.
2. `⭐ 나`가 목표평속 기준으로 자동 생성되고 환산 주행시간이 표시되는지 확인한다.
3. 새 참가자를 추가해 `목표 주행시간`을 선택하고 시/분을 지정한다.
4. 또 다른 참가자를 추가해 `목표 평속`을 선택하고 km/h 값을 지정한다.
5. 각 참가자마다 서로 다른 보급소/PASS와 정차시간을 지정한다.
6. 참가자 목록에서 시간 기준 참가자는 목표시간+환산평속, 평속 기준 참가자는 목표평속+환산시간이 표시되는지 확인한다.
7. 시뮬레이션을 재생해 참가자별 목표와 보급 설정에 따라 순위/정차가 독립적으로 움직이는지 확인한다.
