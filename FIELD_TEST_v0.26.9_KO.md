# v0.26.9 비상충전 후보 선택 + 완주 시뮬레이션 테스트

## 핵심 회귀
1. 설정 → 테스트 모드 → 충전 시나리오 시뮬레이터 진입.
2. 가상 km/SOC를 낮게 잡고 `Kakao 비상 충전 후보 검색` 실행.
3. 팝업에 설명만 보이는 것이 아니라 후보 버튼들이 실제로 표시되는지 확인.
4. 후보 버튼 하나를 누르면 팝업이 닫히고 `비상충전 우회 시뮬레이션` 패널이 나타나는지 확인.
5. `1단계 ▶ 충전소까지 이동` → `2단계 ▶ 목표 SOC까지 충전` → `3단계 ▶ 원래 GPX 이탈점 복귀` 순서로 누른다.
6. 마지막에 COMPLETE 상태가 유지되고, 이탈점 km/SOC/시각 및 아래 원 코스 ETA가 재계산되어 보이는지 확인.
7. `완료 · 비상 시뮬레이션 닫기`로 종료.

## 실제 주행 후보 팝업 회귀
- 긴급충전 판정에서 후보 검색 시 동일한 후보 버튼 목록이 나타나야 한다.
- 후보 선택 후 최종 확인 → `이탈점 고정 · 길안내` 기존 흐름이 유지되어야 한다.

## 데이터 격리
- 시뮬레이터에서 어떤 단계를 진행해도 실제 주행로그/개인학습/Proto/FIT/RideReplanStore에 기록되면 안 된다.
