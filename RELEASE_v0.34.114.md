# TimeGate v0.34.114

## 카메라 계측 운영자 모드 1차 통합

- 일반 첫 화면에서 `CAMERA GATE BETA` 공개 진입 버튼을 제거했습니다.
- RACE 화면의 카메라 게이트 진입은 서버에 등록된 **관리자폰**에서만 표시됩니다.
- 버튼 명칭을 테스트용 표현 대신 `운영자 계측 · START / CP / FINISH`로 정리했습니다.
- 비관리자폰에서는 기존 버튼이 남아 있더라도 화면 갱신 시 제거됩니다.
- Camera Gate Activity는 Android Manifest에서 계속 `exported=false`로 유지되어 외부 앱/웹 링크에서 직접 실행할 수 없습니다.
- 기존 Camera Gate v16의 AUTO / START / CP1~CP5 / FINISH 역할 선택, 보정시각 전송, 0.1초 공식 표시는 그대로 유지합니다.

## 행사 운영 권장

3대 운영 시에는 각 운영자폰을 관리자폰으로 등록한 뒤 RACE 화면의 `운영자 계측`으로 진입합니다. 실제 경기에서는 역할 자동 재배정보다 START / CP1 / FINISH를 명시적으로 선택해 고정하는 방식을 권장합니다.
