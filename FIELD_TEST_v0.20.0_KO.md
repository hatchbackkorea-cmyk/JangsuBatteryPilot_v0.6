# v0.20.0 현장 테스트

1. ECO → 녹색 큰 `ECO` 한 개만 보이는지 확인.
2. AUTO → 왼쪽 파란 `AUTO`, 오른쪽 작은 `추정 + ○○급` 영역이 보이는지 확인.
3. TRAIL → 노란색 큰 `TRAIL` 한 개만 보이는지 확인.
4. TURBO → 주황색 큰 `TURBO` 한 개만 보이는지 확인.
5. 모드를 바꿀 때 배너가 살짝 커졌다 원래 크기로 돌아오는지 확인.
6. AUTO 추정은 실제 Avinox 내부 단계가 아니라 실험값. 자유주행은 SOC/km+가속, 계획주행은 코스경사도 보조 사용.
7. 라이딩 후 ZIP의 events.jsonl에 `AUTO_ASSIST_ESTIMATE`가 남는지 확인.
