# GPX Battery Copilot v0.32.7

## 핵심 업데이트
- 앱 첫 화면에서 Rider Control Center PC 서버의 실제 `/api/health` 응답을 확인합니다.
- 정상 PC 서버이면 `PC 서버 연결 · v버전`을 초록색으로 표시합니다.
- 예전 서버가 응답하면 `구버전 · 업데이트 권장`으로 구분합니다.
- 서버가 꺼져 있거나 외부에서 닿지 않으면 `PC 서버 응답 없음 · 앱 단독 사용 가능`으로 표시합니다.
- Rider Control Center 동기화 대기 건수를 첫 화면에서 함께 확인할 수 있습니다.
- 자동동기화가 끝난 뒤 서버 상태와 대기 건수를 다시 갱신합니다.
- 관리자폰 등록 성공 직후에도 서버 상태를 즉시 다시 확인합니다.

## 기존 기능 유지
- v0.32.6의 ROAD 그란폰도 내부 공개 그룹방 목록 유지
- 이름만 입력하는 공개방 게스트 참가 유지
- 방 GPX 자동 다운로드/적용 유지
- 일반 사용자 안정판 자동 확인 및 수동 업데이트 유지
- 관리자폰 전용 관리자/Beta·RC 기능 유지
- 서버가 없어도 eMTB/ROAD의 로컬 주행 기능은 계속 사용할 수 있습니다.

## 권장 서버
- Rider Control Center PC `0.6.3-pc-admin-safe` 계열
- 새 PC entrypoint 서버는 `/api/health`의 `pc_entry=true`로 구분합니다.

## 현장 확인
1. PC 서버 ON 상태에서 앱 첫 화면에 서버 버전이 표시되는지 확인합니다.
2. PC 서버를 끄면 `응답 없음 · 앱 단독 사용 가능`으로 바뀌는지 확인합니다.
3. 동기화 대기 자료가 있으면 대기 건수가 표시되고 전송 후 줄어드는지 확인합니다.
4. ROAD 공개 그룹방 참가와 GPX 자동 적용이 v0.32.6과 동일하게 동작하는지 확인합니다.
5. 관리자폰에서 관리자 메뉴가 계속 표시되는지 확인합니다.
