# v0.30.1 현장 테스트

## 시작 선택
1. 앱 실행 → `eMTB` 또는 `ROAD` 선택.
2. eMTB는 기존 배터리 코파일럿 기능을 그대로 사용합니다.
3. ROAD는 그란폰도 페이스 코치로 진입합니다.

## ROAD 개인 코칭
1. GPX 코스를 선택합니다.
2. 선택 사항으로 기존 FIT 여러 개와 1/5/20/60분 파워를 넣습니다.
3. 목표 완주시간을 `06:30` 형식으로 입력하고 `계획 만들기`.
4. 10km 간격, GPX POI, 주요 업힐, FINISH의 목표 통과시간을 확인합니다.
5. 주행 시작 후 `목표보다 빠름/늦음`, 다음 포인트 목표시각, 예상 FINISH를 확인합니다.

## 그룹라이딩 최대 20명
1. 팀원 모두 같은 GPX 사용.
2. 한 명이 `6자리 방 코드 만들기` → 팀원에게 공유.
3. 모두 같은 공개 HTTPS 릴레이 URL + 같은 방 코드 + 각자 닉네임 입력.
4. 주행 시작 → `그룹 연결`.
5. 같은 코스 팀원의 앞/뒤 거리와 진행 km가 약 10초 간격으로 갱신되는지 확인.
6. 20명이 참가한 방에는 21번째 신규 참가자가 `방 정원 20명이 가득 찼습니다.`로 거절되는지 확인.

## 배터리 정밀분석 v2
- 상태 버튼 1회당 직전 8초 + 이후 20초의 FFF4 전체 패킷을 저장합니다.
- 캡처마다 읽기 가능한 GATT READ를 1회 수집합니다.
- 충전 중 SOC가 빠르게 변해도 시작/종료 SOC와 전체 패킷이 남습니다.
