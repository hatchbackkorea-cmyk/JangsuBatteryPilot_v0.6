# v0.31.6 — Rider Control Center 자동동기화
- Android 앱과 PC 관리자 서버를 Device Token으로 연결.
- 라이더 프로필/로드 파워커브/앱 상태를 자동 동기화.
- GPX 코스, 로드 페이스 계획, 주행 ZIP을 변경 시 자동 업로드.
- 네트워크 실패 시 로컬 대기열 유지 및 앱 복귀 때 재전송.
- client_key 기반 코스/계획/주행 중복 업로드 방지.
- 기본 내장 GPX도 페이스 계획 생성 전에 자동 등록.
- Device Token은 Android Keystore로 암호화 저장.

# v0.31.5

- Strava ROAD 분석의 최근 30개 제한을 제거하고 전체 활동 목록을 끝까지 페이지 탐색하도록 변경했습니다.
- 각 ROAD 활동의 스트림 분석 결과를 후보 데이터에 캐시해 API 한도(429)에 걸려도 다음 실행에서 미분석 활동부터 이어서 분석합니다.
- 전체 분석이 끝나기 전에는 Strava 프로필 연동 버튼을 비활성화합니다.
- 전체 기록을 연도별로 분리해 15초/1분/2분/5분/10분/20분/40분/1시간/2시간/4시간 PR을 생성합니다.
- 각 PR 옆에 PR이 나온 원본 활동 날짜와 활동명을 표시합니다.
- `역대 PR`과 선택한 `해당 연도 PR`을 함께 비교할 수 있습니다.
- 연도 드롭다운에서 2026/2025/2024 등 사용할 시즌을 직접 고른 뒤 승인 연동합니다.
- 긴 휴식 기록은 기존 규칙대로 장거리 지속능력에서 제외하며, 1시간 이상 PR은 연속 장거리 판정 활동에서만 인정합니다.
- Strava 연동 후에도 목표시간/목표평속/컷오프 계획은 자동으로 변경하지 않습니다.

# v0.31.4

- `내 Strava ROAD 분석 · 선택 연동` 기능을 다시 추가했습니다.
- Strava에서 최근 `sport_type=Ride` ROAD 기록 최대 30개를 불러와도 즉시 앱에 반영하지 않고 **분석 후보**로만 저장합니다.
- MTB / Gravel / e-bike / VirtualRide는 ROAD 분석에서 제외합니다.
- 각 활동의 이동시간/경과시간/총 정차/최대 정차를 분석해 `연속 장거리`, `구간·파워만`, `제외`로 자동 분류합니다.
- 긴 휴식(최대 15분 이상, 총 30분 이상, 정차비율 20% 이상)은 장거리 지속능력 자료에서 제외하고 움직인 구간/파워 자료로만 사용합니다.
- 1시간 이상 파워 PR은 긴 휴식 없는 `연속 장거리` 기록에서만 후보로 사용합니다.
- 분석 결과를 확인한 뒤 **이 분석을 연동** 버튼을 눌러야 활성 라이더 프로필로 저장됩니다.
- Strava 분석을 연동해도 현재 목표시간/평속/컷오프 설정을 자동으로 덮어쓰지 않습니다.
- 예전 v0.30.4 Strava 인증 저장소와 같은 Client ID/Keystore 키를 사용해 기존 연결 정보가 남아 있으면 재사용할 수 있습니다.
- OAuth 권한은 읽기 전용 `activity:read_all`만 요청합니다.

# v0.31.3
- ROAD 레이스 시뮬레이터 참가자 목록을 참가자 1명 = 카드 1개 UI로 변경.
- 각 카드에서 이름/목표기준/평속/주행시간을 분리 표시하고 카드별 수정 버튼 제공.
- 보급 목록은 기본 접힘 상태이며 `보급 N곳 ▼`를 눌러 펼치거나 접을 수 있음.
- 펼친 보급 목록은 보급소 1곳당 정확히 1행으로 표시하고, 정차시간은 우측 정렬.
- 기존 전역 `참가자 수정` 버튼 제거, 카드별 수정으로 단순화.

# v0.31.2 — 참가자 보급소 목록 한 줄 정리

- 참가자 목록에서 보급소/정차시간 표시를 한 줄씩 분리
- 여러 보급소가 한 줄에 이어져 보이던 가독성 문제 수정
- 기존 컷오프/목표평속/목표시간 기능 유지
