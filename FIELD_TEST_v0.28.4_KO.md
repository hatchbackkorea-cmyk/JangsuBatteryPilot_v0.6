# v0.28.4 현장 점검 — 비교 모드별 자체 SOC

1. 같은 GPX를 불러옵니다.
2. Avinox 기준값에 ECO/AUTO/TRAIL/TURBO 값을 입력합니다.
3. 비교 모드를 ECO로 저장하고 테스트 모드에서 0km/중간/종점 자체 SOC와 `자체 ECO` 라벨을 확인합니다.
4. AUTO, TRAIL, TURBO로 각각 변경해 저장한 뒤 같은 지점에서 자체 SOC가 해당 모드의 학습값으로 다시 계산되는지 확인합니다.
5. Avinox 쪽 값도 선택한 모드로 바뀌는지 함께 확인합니다.
6. 모드별 A급 학습 데이터가 전혀 없는 경우 중립 fallback 때문에 값 차이가 작거나 같을 수 있습니다. 이 경우에도 `자체 ECO/AUTO/TRAIL/TURBO` 라벨은 선택 기준을 정확히 표시해야 합니다.
7. 실제 계획주행에서는 BLE 모드가 HIGH/CONFIRMED로 검증되면 실제 모드가 자체 예측 기준을 이어서 갱신하는지 확인합니다.

※ Avinox 예상 소비량 숫자는 자체 모델에 직접 섞지 않습니다.
