# v0.18.2 BLE HUD + Avinox 모드 자동감지 검증

## 이번 버전에서 확인할 것
1. `주행 시작 → 임의주행` 후 상단 큰 `배터리` 숫자가 자전거 Avinox SOC와 일치하는지 확인합니다.
2. 별도 `AVINOX 실제 배터리` 카드는 더 이상 표시하지 않습니다. 큰 배터리 카드가 BLE 실제 SOC를 담당합니다.
3. Eco / Auto / Trail / Turbo 4개 큰 수동 버튼은 숨겼습니다.
4. 자전거 모드를 바꾸면 상단 `Avinox 모드` 표시가 BLE에서 자동 갱신되는지 자전거 화면과 비교합니다.
5. `Eco`와 일부 `Auto`는 강한 후보로 바로 표시됩니다.
6. 현재 확인된 FFF4 필드에서 `2/3`은 Trail/Turbo와 Auto 동적상태가 겹칠 수 있어 `Trail / Auto`, `Turbo / Auto`처럼 검증 후보로 표시될 수 있습니다.
7. 후보의 첫 모드가 자전거와 맞으면 `✓ ... 맞음`을 한 번 누릅니다. 다르면 `다름 · 실제모드 선택`에서 자전거의 실제 모드를 선택합니다.
8. 사용자 확인된 구간과 HIGH 구간만 클린 모드 학습 후보가 되고, `AMBIGUOUS` 구간은 자동 학습에서 제외됩니다.
9. 종료 후 `최근 로그 ZIP`을 저장해 보내주세요. `assist_auto_detect.csv`에 감지 후보 + raw code + 전체 FFF4 long packet이 주행 내내 기록됩니다.

## 배터리 카드 표시
- BLE 정상: `배터리 72%` + `● BLE 실제 · 계획 74% · 오차 -2% ...`
- BLE 재연결: 마지막 실측값을 유지하되 `BLE 재연결 중`으로 표시
- BLE 실측이 한 번도 없으면 `—` 표시, 계획값은 아래 작은 문구에만 표시
- BLE가 끊긴 동안에만 상단 배터리 옆 `수동` 버튼이 나타납니다.

## 중요
이번 버전의 모드 자동감지는 아직 `selected_mode` 최종 확정판이 아니라 **현장 검증판**입니다. 배터리 SOC 때처럼 자전거 실제 표시와 대조하면서 후보 필드를 검증합니다. 애매한 구간은 학습에 넣지 않아 데이터 오염을 막습니다.
