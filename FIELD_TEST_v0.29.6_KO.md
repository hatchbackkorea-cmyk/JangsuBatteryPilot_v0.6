# v0.29.6 배터리 정밀 분석 현장 테스트

## 업데이트 직후
1. 메인 화면을 오른쪽 끝까지 넘겨 `배터리` 페이지로 이동합니다.
2. `기존 Avinox 원본 다시 분석`을 한 번 누릅니다. 이미 저장된 원본이 있으면 즉시 요약이 나옵니다.
3. `배터리 정밀 분석 열기`로 들어갑니다.

## 테스트는 연속일 필요 없음
- `시작 / 이어하기`를 한 번 누른 뒤 상태 버튼을 시간 날 때 하나씩 누르면 됩니다.
- 앱을 닫거나 몇 시간/며칠 뒤 다시 들어와도 `세션 종료`를 누르지 않았다면 같은 세션에 이어집니다.
- 권장 순서: 대기 → 주행/부하 → 충전기 연결 → 충전 중 → 충전 제한 변경 → 충전 종료 → 좌/우 스위치.
- 한 번에 다 못 해도 됩니다.

## 각 상태에서 할 일
- 자전거/충전기 상태를 실제로 바꾼 뒤 10~20초 정도 BLE가 안정되면 해당 상태 버튼을 1회 누릅니다.
- 같은 상태라도 SOC가 달라졌다면 한 번 더 눌러도 됩니다.
- 주행 중에는 RideService가 BLE를 계속 수집하므로 별도 BLE 연결을 만들지 않습니다.

## 로그 보내기
- 테스트가 어느 정도 모이면 `테스트 로그 ZIP 공유`로 내보냅니다.
- 세션을 완전히 끝내고 싶을 때만 `세션 종료`를 누릅니다.

## 주의
- `관측 방전 환산`은 BMS 실제 cycle count가 아닙니다.
- BMS 실제 cycle/SOH/전압/셀 밸런싱 등의 숫자는 검증될 때까지 미해독으로 남습니다.
