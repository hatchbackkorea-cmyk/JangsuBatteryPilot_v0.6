# v0.17.0 Avinox 모드/프로필 현장 테스트

## 라이딩 전 1분 준비
1. 주행 화면의 `⚙ 설정값`을 눌러 Eco / Auto / Trail / Turbo 값이 Avinox 앱과 같은지 확인합니다.
2. 사진에서 숫자가 보이지 않는 `모터 오버런 / 스타트 어시스트 / 연속 어시스트`는 0~4 상대 위치로 기록합니다. 왼쪽 끝=0, 오른쪽 끝=4입니다.
3. 출발할 실제 모드 버튼을 한 번 눌러 `시작 예정`으로 맞춥니다.

## 라이딩 중
1. `주행 시작` → `임의주행`을 권장합니다. GPX와 독립적으로 검증하기 쉽습니다.
2. 자전거에서 모드를 바꾼 직후 앱에서도 같은 모드 버튼을 누릅니다.
3. 버튼을 누른 뒤 12초 동안 Avinox FFF4 원시 BLE 패킷이 자동 수집됩니다.
4. 모드별 소비 검증을 위해 가능하면 한 모드를 5분 이상 유지하고, BLE SOC가 최소 1% 떨어지는 구간을 확보합니다.
6. 모드 설정값 자체를 Avinox 앱에서 바꿨다면 `⚙ 설정값`에서 같은 값을 저장합니다. 현재 모드라면 새 profileId로 즉시 새 구간이 시작됩니다.

## 라이딩 종료 후 보내줄 것
주행 종료 후 최근 ZIP을 보내주세요. ZIP 안에는 다음이 들어갑니다.
- `*.csv`: GPS + SOC + assist_mode + assist_profile_id
- `*_assist_profiles.jsonl`: 실제 사용 프로필 스냅샷
- `*_raw_ble_probe.csv`: 모드 전환 직후 12초 원시 FFF4 패킷
- `*.json`: `assistModeStats` 포함 종합 결과

## 데이터 품질 규칙
모드별 SOC 소비는 모드 변경이 끼지 않은 연속 BLE SOC 하락만 해당 모드에 귀속합니다. 모드 변경을 사이에 둔 1% 하락은 어느 모드 소비인지 확정할 수 없으므로 자동 제외합니다. 이 데이터는 검증 단계이며 개인 배터리 학습에 자동 반영하지 않습니다.

## v0.17.2 정책
- BOOST는 순간 출력 기능으로 보고 모드 버튼/프로필/모드별 통계 대상에서 제외합니다.
- 실제 BOOST 사용으로 발생한 배터리 감소는 전체 주행 SOC에는 자연스럽게 포함되지만 BOOST 전용 학습값은 만들지 않습니다.
