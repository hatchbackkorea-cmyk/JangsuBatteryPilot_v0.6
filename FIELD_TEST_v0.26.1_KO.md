# v0.26.1 Proto A+ 학습 UI 현장 테스트

## 첫 설치 후
1. Shizuku 앱을 실행하고 `실행 중` 상태를 확인합니다.
2. Battery Copilot → `학습` → `과거 라이딩 학습`으로 이동합니다.
3. 상단 `Avinox 원본 A+ 학습`에서 `Shizuku 권한 허용`을 누릅니다.
4. 처음에는 `과거 Proto 전체 동기화`를 눌러 기존 Avinox 원본을 모두 소급 학습합니다.
5. 이후에는 `새 Proto 동기화`만 사용하면 됩니다. 앱 복귀 시에는 새 원본 최대 8개를 자동 검색합니다.

## 정상 표시 예
- `Avinox 원본 자동동기화 준비됨`
- `원본 41개 · A+ 채택 ...`
- `총 ... km · A+ 학습 ...구간`

같은 버튼을 다시 눌렀을 때 `새 Avinox 원본 없음`이면 중복 방지가 정상입니다.

## 누적 에너지 비교 UI
- 큰 소비값은 첫 줄
- `(자체 ...%)`, `(AUTO ...%)` 같은 보조 예상값은 둘째 줄
- 세 칸 어느 곳에서도 오른쪽 글자 잘림이 없어야 합니다.

## 주의
주행 기록 중에는 과거 원본 동기화를 실행하지 않습니다. Avinox 앱 활동 동기화가 끝난 뒤 새 Proto 동기화를 실행하세요.
