# GPX Battery Copilot v0.32.6

## 일반 사용자 업데이트
- 앱 시작 화면에서 하루 1회 GitHub Releases 안정판 자동 확인
- 일반 사용자도 `앱 업데이트 확인` 버튼으로 즉시 확인 가능
- 새 버전이 있으면 APK 다운로드 → SHA-256/패키지 검증 → Android 설치 확인 화면
- 관리자 비밀번호/Device Token 없이 업데이트 가능
- 관리자 전용 메뉴와 Beta/RC 관리 기능은 관리자폰에만 유지

## 그룹방 위치 변경
- 첫 모드 선택 화면의 `지금 참가 가능한 그룹방` 패널 제거
- `ROAD → 그란폰도 페이스 코치` 화면 최상단으로 이동
- 공개방 선택 → 이름 입력 → 임시 방 토큰 자동 발급 → 즉시 WebSocket 참가
- 방에 GPX가 있으면 기존 v0.32.4/0.32.5 방식으로 자동 다운로드 및 적용
- 일반 사용자는 수동 Device Token 설정이 필요 없음

## 서버
- Rider Control Center PC v0.6.1 계열 API와 호환
- 서버 업데이트 없이 앱만 v0.32.6으로 올릴 수 있음
