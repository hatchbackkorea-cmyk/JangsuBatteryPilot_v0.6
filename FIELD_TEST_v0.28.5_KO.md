# v0.28.5 현장/테스트 확인

1. GPX 선택 후 코스 2페이지를 엽니다.
2. `모드별 주행 전략` 표의 열이 `ECO / AUTO / TRAIL / TURBO` 순서인지 확인합니다.
3. 각 포인트 셀에 `도착시각`과 `예상 SOC`가 함께 보이는지 확인합니다.
4. 등록 충전소에서는 `도착 SOC→계획 충전 SOC`로 보이는지 확인합니다.
5. Avinox 비교 모드를 바꾸면 해당 모드 열만 강조되고, 네 모드 자체 값은 각각 독립적으로 유지되는지 확인합니다.
6. 테스트 모드에서 GPX 위치를 이동하면 현재 위치 이후의 전략표가 다시 계산되는지 확인합니다.
7. 모드별 학습이 있는 경우 ECO/AUTO/TRAIL/TURBO의 SOC와 ETA가 서로 다르게 계산되는지 확인합니다.
8. 학습이 부족한 모드는 헤더/안내에 `※`가 나타나고 임의로 가짜 속도 차이를 만들지 않는지 확인합니다.
9. 표 맨 아래 `전체 남은 전략`에서 총시간 / 정차시간 / 종점 SOC가 각 모드별로 표시되는지 확인합니다.
10. Avinox 외부 예상 소비량 숫자를 수정해도, 동일한 개인학습 상태에서 전략표의 우리 모델 계산식에 그 숫자가 직접 주입되지 않는지 확인합니다.
