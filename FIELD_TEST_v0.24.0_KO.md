# v0.24.0 SRAM 실험실 빠른 테스트

1. 앱 실행 후 화면을 넘겨 **설정** 페이지로 이동합니다.
2. `실험 / 앱 업데이트` 영역에서 **🧪 SRAM 실험실 · AXS/T-Type 스캔**을 누릅니다.
3. SRAM 실험실에서 드레일러를 깨운 뒤 `SRAM 검색`을 누릅니다.
4. `SRAM 1324501905`처럼 SRAM으로 보이는 장치를 선택합니다.
5. 연결 준비 완료 후 `캡처 시작` → 실제 SRAM 버튼으로 여러 단 변속 → `종료 · 분석`을 누릅니다.
6. 후보 UUID가 나오면 `진단 로그 TXT 저장`으로 저장합니다.

※ v0.24.0 SRAM 실험실은 읽기 전용입니다. 앱이 변속 WRITE 명령을 보내지는 않습니다.
