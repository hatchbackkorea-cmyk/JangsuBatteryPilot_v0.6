# v0.22.0 현장 확인

## 1. FIT 단독 B급 보조학습
1. 설정 → `검증 학습 관리 · FIT + 앱 ZIP`.
2. `🟦 FIT 단독 보조학습 · 여러 개 선택`을 누릅니다.
3. 과거 Avinox `.fit`을 여러 개 선택합니다.
4. 결과에 `B급 FIT 보조학습` 성공 파일 수와 보조구간 수가 표시되는지 확인합니다.
5. 학습 목록에서 `🟦 B급 FIT 보조학습`으로 표시되는지 확인합니다.
6. B급은 거리/고도/속도/Rider/Motor Power/Cadence만 사용하며 SOC·모드별 배터리 소비계수는 수정하지 않습니다.

## 2. A급 검증학습
- 기존처럼 같은 주행의 FIT + 앱 ZIP을 함께 선택합니다.
- 거리/고도/파워는 FIT, BLE SOC/선택모드는 ZIP을 정답으로 사용합니다.
- A급은 배터리 소비모델에 직접 반영됩니다.

## 3. 충전 권장 기준
1. 설정의 기존 `종점 목표 잔량`이 `충전 권장 기준`으로 표시되는지 확인합니다.
2. 예: 15%는 종점에 15%를 남기기 위한 기준입니다.
3. 계획주행 중 등록된 충전지점에서 `충전 시작`을 누릅니다.
4. 화면의 `앱 권장 xx%`가 현재 소비보정과 남은 구간, 충전 권장 기준으로 계산되는지 확인합니다.

## 4. 충전 알림
- 임의주행: 설정의 기본 충전 알림 50~100% 사용.
- 계획주행: 충전지점의 `앱 권장 SOC`가 알림 목표로 자동 우선.
- 목표 도달 시 알림 + 진동 + 음성 안내.
- 알림만 하며 충전은 자동 중단하지 않습니다.
- 계속 충전하면 100%에서 한 번 더 알립니다.
