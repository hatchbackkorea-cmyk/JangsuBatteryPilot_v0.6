# v0.15.0 임의주행 현장 테스트

1. 앱 첫 화면에서 `주행 시작` → `🚵 임의주행` 선택
2. 출발 직후 마이크를 눌러 `배터리 100프로야`처럼 시작 잔량 입력
3. 평소처럼 자유롭게 주행. GPX는 사용하지 않음
4. 가능하면 10~20km 또는 큰 업힐 전후마다 현재 배터리 잔량 입력
5. 중간 충전 시 `충전 시작`에서 충전 전 잔량 → 충전 후 `충전 완료`에서 잔량 입력
6. 라이딩 종료 시 `주행 종료` → 저장
7. Avinox에서 해당 라이딩 FIT 다운로드
8. 앱 `피드백` → `최근 주행에 Avinox FIT 연결`
9. Avinox 내비게이션에 실제 주행 경로를 등록해 ECO/AUTO/TRAIL/TURBO 예상 소비량 확인
10. 앱 `최근 주행 Avinox 예상값 입력`에서 입력하고 대표 비교 모드 선택
11. `실제 · 자체모델 · Avinox 비교`에서 오차 확인
12. 비교를 확인한 뒤 원하는 경우 `최근 임의주행을 개인 학습에 반영`

## 현장 체크 포인트
- 임의주행 상단에 `GPX 독립` 표시가 나오는지
- 선택된 기존 GPX의 코스이탈 경고가 나오지 않는지
- 이동거리가 실제와 대략 맞게 증가하는지
- 배터리 음성 입력 후 현재 실제값과 누적 소비량이 바뀌는지
- 충전 후 배터리 잔량은 올라가도 누적 소비량이 감소하지 않는지
- 주행 종료 후 FIT을 붙이기 전에는 자체 사후예측이 생성되지 않는지
- FIT 연결 후 `우리 모델(FIT 기준·학습 전)` 값이 생기는지
- Avinox 값을 넣어도 개인 학습 개수가 자동 증가하지 않는지
- `학습 반영`을 직접 누른 뒤에만 개인 학습이 증가하는지
