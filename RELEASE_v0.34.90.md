# TimeGate v0.34.90

## Camera Gate Beta 1차 검증판

- 메인 선택 화면에 `CAMERA GATE BETA` 진입 버튼 추가
- GPS/지도와 완전히 분리된 카메라 START 트리거 테스트 화면 추가
- Android 기본 Camera2로 후면 카메라의 60 FPS 범위를 우선 요청하고 실제 분석 FPS를 실시간 표시
- 화면 중앙 가상 START LINE 통과 모션을 Y 밝기 변화로 감지해 트리거 생성
- 트리거 순간의 카메라 프레임 시각, 폰 내부 시각, 서버 보정 시각을 밀리초 단위로 표시
- 서버와의 시계 오프셋, 최저 RTT, 동기화 추정 오차 표시
- 정밀 `/api/race/clock` API가 있으면 우선 사용하고, 없으면 HTTP Date 다중 샘플 교차추정으로 자동 폴백
- 감지 임계값(민감도) 조정 및 최근 20개 트리거 로그 표시
- 카메라 권한 추가

### 주의

이번 버전은 계측 정확도 검증 전용 베타입니다. 기존 GPS RACE 계측 및 공식 기록에는 아무 데이터도 기록하지 않습니다.
