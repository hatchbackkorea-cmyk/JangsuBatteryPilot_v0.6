# v0.25.0 현장 확인

## 첫 화면
1. 상단의 `주행 대기 / 시작 버튼에서 주행 방식 선택` 안내가 사라졌는지 확인합니다.
2. Avinox 모드 카드 아래 안내/`설정값` 행이 사라졌는지 확인합니다.
3. 현재 거리/배터리 카드에서 `BLE 연결 대기` 설명 행이 사라졌는지 확인합니다.
4. 누적 에너지 비교에서 하단 긴 설명이 사라지고 `현재 소비% (자체 종점예상%)`, `현재 소비% (AUTO 종점예상%)` 형태로 보이는지 확인합니다.
5. 여유 카드가 `기준 20% · 여유 5%`처럼 두 값을 한 줄에 표시하는지 확인합니다.

## FIT 자동 폴더
1. 설정 > 배터리 학습 데이터 > `FIT 자동 폴더 지정`을 누릅니다.
2. Avinox FIT을 저장/복사하는 사용자 접근 가능 폴더를 한 번 선택합니다.
3. `지금 자동검색`으로 첫 검사를 실행합니다.
4. 이후 앱을 다시 열거나 복귀하면 새 `.fit`만 자동 B급 보조학습합니다.
5. 자동 학습은 Rider/Motor Power·Cadence·지형 보조값만 반영하며 SOC/모드 소비계수는 바꾸지 않습니다.

※ Avinox 앱의 비공개 내부 저장소는 Android 보안상 폴더 선택기에 나타나지 않을 수 있습니다. 그런 경우 사용자가 접근 가능한 FIT 저장 폴더를 지정해야 합니다.
